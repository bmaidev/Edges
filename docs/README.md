# Edges — documentation

Everything you need to run a workshop on the platform.

## Start here

If you're **setting up** (getting in, creating rooms, sharing access):
→ **[Admin guide](admin-guide.md)**

If you're **running a session** (driving the room live):
→ **[Facilitator guide](facilitator-guide.md)**

## Reference

- **[Roles & passcodes](roles-and-passcodes.md)** — how access works: your
  workspace, the people in it, and the four roles inside a room (admin,
  facilitator, co-host, projector) and exactly what each can do.
- **[Module reference](modules.md)** — every facilitation tool, in plain
  language: what participants do, what you see, and its key settings.
- **[Templates catalog](templates.md)** — the ready-made, one-tap sessions and
  the module flow inside each.
- **[AI features & privacy](ai-and-privacy.md)** — what the optional AI does
  across setup → live → report, and the full privacy model.

## The shape of it (90 seconds)

1. You get a **workspace** — your own space for your rooms and your people. Create
   one at **`/start`**, or an organiser hands you a **sign-in link** to one that
   already exists. (One person, a team, or a whole org can each have their own.)
2. You **sign in** at `/admin` — usually just by clicking your bookmarked link.
   This is your console: create rooms, brand them, invite teammates, see reports.
3. Inside the console you create a **room** and get three room passcodes (admin /
   facilitator / co-host) plus the join, host, and projector URLs — shown **once**.
4. **Participants** open the join URL on their phones (`/r/<room>`) and pick a
   handle (or stay anonymous). No app, no account.
5. A **facilitator** opens `/r/<room>/host`, picks a template (or builds a custom
   session), and drives it phase by phase. The room watches the **projector** at
   `/r/<room>/screen`.
6. A **session** is a sequence of **module** phases. Each module is one tool —
   capture, a poll, a dot-vote, a World Café round, an AI devil's-advocate, etc.
7. At the end, **Archive** wipes the live data and (if an AI key is set) saves a
   synthesised report you can read back in your console.

Nothing is kept beyond 24 hours, and raw contributions are facilitator-only.
